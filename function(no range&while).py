# 初始化变量 a 和 b
a = -50
b = 0

# 遍历从 -50 到 50 的所有整数
a = -50
while a <= 50:
    # 计算函数值 b = a^2 + 2a + 1
    b = a**2 + 2*a + 1

    # 打印 a 和 b 的值
    print(f"a = {a}, b = {b}")

    # 增加 a 的值
    a += 1  