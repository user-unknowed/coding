# 学习 C/C++ 的一些记录
## 未知
其实 C++ 学习起来还是比较简单的，因为 C++ 是在 C 语言的基础上发展起来的，并且我之前也学习过 C 语言，所以 C++ 学习起来比较快。

## 2026/2/10
语法上除了cin和cout，其他的语法都和 C 语言是一样的。甚至还有更多的数据包和函数可以使用，如队列。

## 2026/2/14
我在学习队列时，发现队列的操作和栈是不同的，队列是先进先出，而栈是先进后出。（其实栈不是很懂，但是对于队列的操作，其实就像水杯一样，先进去的先出来）

## 2026/3/2
好久之前就下载了Docker-Desktop,但是发现每次打开都不会登录，md烦死了。

## 2026/3/3
今天学了struct结构体，但是代码实践的时候有点问题，但是有一点理解的，其实就是数组，但是可以包含和使用的范围更广，就好像python里的哈希表，但是和哈希不同的是，哈希表是通过键值对来存储数据的，而结构体是通过成员变量来存储数据的，而且结构体可以包含不同类型的成员变量，而哈希表只能包含键值对。
现在的问题是这个结构体和一般的数字可不可以进行加减乘除等操作？显然说在课件里结构体之间的是可以的，但是结构体和一般的数字是不可以进行加减乘除等操作的，因为结构体是一种自定义的数据类型，而一般的数字是一种基本的数据类型。