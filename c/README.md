# 学习 C/C++ 的一些记录
## 未知
其实 C++ 学习起来还是比较简单的，因为 C++ 是在 C 语言的基础上发展起来的，并且我之前也学习过 C 语言，所以 C++ 学习起来比较快。

## 2026/2/10
语法上除了cin和cout，其他的语法都和 C 语言是一样的。甚至还有更多的数据包和函数可以使用，如队列。

## 2026/2/14
我在学习队列时，发现队列的操作和栈是不同的，队列是先进先出，而栈是先进后出。（其实栈不是很懂，但是对于队列的操作，其实就像水杯一样，先进去的先出来）

## 2026/3/2
好久之前就下载了Docker-Desktop,但是发现每次打开都不会登录，md烦死了。

## 2026/3/3
今天学了struct结构体，但是代码实践的时候有点问题，但是有一点理解的，其实就是数组，但是可以包含和使用的范围更广，就好像python里的哈希表，但是和哈希不同的是，哈希表是通过键值对来存储数据的，而结构体是通过成员变量来存储数据的，而且结构体可以包含不同类型的成员变量，而哈希表只能包含键值对。
现在的问题是这个结构体和一般的数字可不可以进行加减乘除等操作？显然说在课件里结构体之间的是可以的，但是结构体和一般的数字是不可以进行加减乘除等操作的，因为结构体是一种自定义的数据类型，而一般的数字是一种基本的数据类型。

## 2026/3/4
今天学了指针，这其实是一种关于内存的一种数量表示方式，它是把十六进制的内存转换为一个变量（形如*ptr），而这个变量就是指针变量，它可以指向内存中的某个地址，从而可以通过指针变量来访问内存中的数据。
指针虽然是地址的一个方法，但是它是否在引用数据的时候，是直接引用数据，还是引用数据的地址，这是一个问题。明天在实践课的时候再试一下是否会改变其用时和占用内存。

## 2026/3/16
今天对指针进行了深度学习，好像这种地址之间的运算比直接用地址内的数据要快，因为地址之间的运算只是简单的加减，而直接用地址内的数据，需要先把地址内的数据取出来，然后再进行运算。但是只用指针又不是万全之策，有时候用指针的时间复杂度还不如直接用数据的时间复杂度低（eg:char类型引用）。

##2026/3/25
我再次理解了为什么说指针难，因为他真的有着很多的形式，比如函数指针（如triagle_compute.c中的p），数组指针（triagle_compute.c中的(*p)[double]）等。这个数组指针是指向一个数组的指针，它可以指向一个数组的任意一个元素，也可以指向一个数组的任意一个元素的地址。这么多的形式换来的是类似与python的方便的操作。
所以指针好哇，得学啊。